---
status: accepted
---

# Host Policy controls Provider activation

Installing or registering Security Assurance does not add a security obligation by itself. The host selects `disabled`, `when-available`, or `required`; a `when-available` Provider selected into a frozen Attempt becomes mandatory for that Attempt, while neither the Provider nor an agent may weaken or mutate the Assurance Plan.

