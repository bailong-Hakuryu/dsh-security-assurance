---
status: accepted
---

# Dependency advisories are versioned offline snapshots

Dependency vulnerability analysis consumes a signed, versioned local Advisory Snapshot whose identity and digest are frozen into Provider Composition. Fetching, verifying, and activating newer intelligence is an explicit administrative update outside every Assessment; an Assessment never substitutes a live latest network response for reproducible Evidence.

