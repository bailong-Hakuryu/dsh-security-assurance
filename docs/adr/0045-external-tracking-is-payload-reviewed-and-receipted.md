---
status: accepted
---

# External tracking is payload-reviewed and receipted

Each Tracking Operation selects sealed Findings, checks destination duplicates, displays the exact payload, authenticated destination identity, visibility, and disclosure risks, then requires single-use authorization before one external write. The operation binds one Provider and destination and records the returned external identity and receipt; general network access never authorizes tracking.

