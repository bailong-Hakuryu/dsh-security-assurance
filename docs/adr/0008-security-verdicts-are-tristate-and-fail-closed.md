---
status: accepted
---

# Security Verdicts are tristate and fail closed

Security Verdict is `SATISFIED` only when every mandatory obligation has complete eligible coverage and no blocking Finding, `FAILED` when Evidence proves a policy violation, and `INDETERMINATE` when missing coverage or operational failure prevents a reliable conclusion. Scores may inform presentation but never replace or relax this tristate decision.

