---
status: accepted
---

# Security Assurance ships as one bundle with independent entry points

Security Assurance is one npm installation unit and one plugin product, but its root Service, model tools, Control Plane Provider adapter, Client Workbench, invariant companion, and generated Typert artifacts are separate entry points. Bundle rows activate those capabilities independently; publication splits into multiple packages only after a real Provider or Consumer must evolve or deploy separately.

