---
status: accepted
---

# Workbench is an additive Harness Web surface

The Workbench Client contributes a launcher to `sidebar.footer.action` and renders the full Security surface through `shell.overlay`. It never replaces the root, sidebar, conversation, or details owners; the Host Service remains independently usable in headless compositions where Client slots are absent.

