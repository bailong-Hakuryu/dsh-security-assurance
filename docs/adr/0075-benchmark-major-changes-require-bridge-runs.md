---
status: accepted
---

# Benchmark major changes require Bridge Runs

Corpus, Ground Truth, Finding Matching Contract, and Benchmark Profile are independently versioned with reviewed reasons for every change. Breaking measurement changes increase Benchmark major version and require a Bridge Run before historical and current results are compared; tests are never removed merely because the product misses them.

